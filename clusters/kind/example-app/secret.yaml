# EXAMPLE VALUES — these are dummy credentials so the cluster is copy-paste
# runnable. Replace both keys before using this pattern anywhere shared.
apiVersion: v1
kind: Secret
metadata:
  name: demo-secrets
  namespace: demo
  labels:
    app.kubernetes.io/name: demo-app
    app.kubernetes.io/part-of: kind-playground
type: Opaque
stringData:
  api-token: "demo-not-for-production-token"
  demo-password: "demo-pass-change-me"
